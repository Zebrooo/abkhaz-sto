// Заглушка пакета "server-only" для тестов. В сборке его подменяет сам Next
// (в зависимостях пакета нет), а vitest без подмены не может импортировать
// серверные модули — куки отметки, контекст, выход. Сторожевая роль
// директивы от этого не страдает: она нужна сборке, а не тестам.
export {};
